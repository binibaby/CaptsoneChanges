# ============================================
# COPY THIS TO RENDER ENVIRONMENT VARIABLES
# ============================================
# In Render dashboard, click "Add Environment Variable"
# For each line below, copy the variable name and value
# Example: Variable = APP_ENV, Value = production
# ============================================

# Laravel Application Configuration
APP_NAME=PetsitConnect
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=
APP_TIMEZONE=UTC
APP_LOCALE=en
APP_FALLBACK_LOCALE=en

# Database Configuration (PostgreSQL on Render)
DB_CONNECTION=pgsql
DB_HOST=dpg-d45cdq95pdvs73burvng-a.oregon-postgres.render.com
DB_PORT=5432
DB_DATABASE=pet_sitting_db
DB_USERNAME=pet_sitting_db_user
DB_PASSWORD=oFwIDD42ZUtxMGIenKQlTOTGLTq0RYLs

# Logging Configuration
LOG_CHANNEL=stack
LOG_LEVEL=error
LOG_DEPRECATIONS_CHANNEL=null

# Session Configuration
SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

# Cache Configuration
CACHE_DRIVER=file
CACHE_PREFIX=

# Queue Configuration
QUEUE_CONNECTION=sync

# Broadcasting Configuration (if using websockets)
BROADCAST_DRIVER=log
REVERB_APP_ID=your-reverb-app-id
REVERB_APP_KEY=your-reverb-app-key
REVERB_APP_SECRET=your-reverb-app-secret
REVERB_HOST=localhost
REVERB_PORT=8080
REVERB_SCHEME=http

# Filesystem Configuration
FILESYSTEM_DISK=local

# Mail Configuration (if using email)
MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# Semaphore SMS Configuration (if using SMS)
SEMAPHORE_API_KEY=
SEMAPHORE_BASE_URL=https://api.semaphore.co/api/v4
SEMAPHORE_SENDER_NAME=Petsit
SEMAPHORE_ENABLED=true
SMS_SIMULATION_MODE=false

# Xendit Payment Configuration (if using payments)
XENDIT_SECRET_KEY=
XENDIT_WEBHOOK_TOKEN=

# Veriff ID Verification Configuration (if using ID verification)
VERIFF_API_KEY=
VERIFF_API_SECRET=

# Stripe Payment Configuration (if using Stripe)
STRIPE_KEY=
STRIPE_SECRET=
STRIPE_WEBHOOK_SECRET=

# Twilio Configuration (if using Twilio)
TWILIO_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# AWS Configuration (if using AWS S3 for file storage)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# ============================================
# INSTRUCTIONS FOR RENDER:
# ============================================
# 1. In Render dashboard, go to your web service
# 2. Click on "Environment" tab
# 3. Click "Add Environment Variable"
# 4. For each variable above:
#    - Copy the variable name (left side of =)
#    - Copy the value (right side of =)
#    - If value is empty, leave it empty or fill with your actual value
#
# IMPORTANT NOTES:
# - APP_KEY: Leave empty - Render will generate it automatically
# - APP_URL: Leave empty for now, update after deployment with your Render URL
# - Fill in API keys for services you're using (Semaphore, Xendit, Veriff, etc.)
# - Remove variables you're not using
# ============================================

